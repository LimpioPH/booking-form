<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Limpio PH Process</title>
    <style>
        body {
            margin: 0;
            padding: 20px;
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        
        .container {
            width: 800px;
            height: 800px;
            background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%);
            position: relative;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 20px 40px rgba(0,0,0,0.2);
        }
        
        .logo-container {
            position: absolute;
            top: 30px;
            right: 30px;
            width: 120px;
            height: 120px;
            background: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        }
        
.logo {
    width: 80px;
    height: 80px;
    object-fit: contain;
}
          
        .title {
            color: white;
            font-size: 48px;
            font-weight: bold;
            text-align: left;
            margin: 80px 0 0 50px;
            line-height: 1.1;
        }
        
        .subtitle {
            color: #bfdbfe;
            font-size: 18px;
            margin: 10px 0 0 50px;
            font-weight: 500;
        }
        
        .steps-container {
            margin: 60px 50px;
            display: flex;
            flex-direction: column;
            gap: 40px;
        }
        
        .step {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            padding: 30px;
            position: relative;
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
            backdrop-filter: blur(10px);
        }
        
        .step-number {
            position: absolute;
            top: -15px;
            left: 30px;
            background: #f59e0b;
            color: white;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 18px;
            box-shadow: 0 4px 10px rgba(245, 158, 11, 0.3);
        }
        
        .step-title {
            color: #1e3a8a;
            font-size: 22px;
            font-weight: bold;
            margin-bottom: 10px;
            margin-top: 5px;
        }
        
        .step-content {
            color: #374151;
            font-size: 16px;
            line-height: 1.4;
        }
        
        .rates {
            margin-top: 15px;
        }
        
        .rate-item {
            color: #1f2937;
            font-size: 15px;
            margin: 5px 0;
            padding-left: 15px;
            position: relative;
        }
        
        .rate-item::before {
            content: '•';
            color: #3b82f6;
            font-weight: bold;
            position: absolute;
            left: 0;
        }
        
        .download-btn {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background: #10b981;
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 8px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            box-shadow: 0 4px 15px rgba(16, 185, 129, 0.3);
            transition: all 0.3s ease;
        }
        
        .download-btn:hover {
            background: #059669;
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(16, 185, 129, 0.4);
        }
    </style>
</head>
<body>
    <div class="container" id="graphic">
        <div class="logo-container">
            <div class="logo">
            <img src="YOUR_GITHUB_LOGO_LINK_HERE" alt="Limpio PH Logo" class="logo">
            </div>
        </div>
        
        <div class="title">OUR PROCESS</div>
        <div class="subtitle">Simple steps to a spotless home</div>
        
        <div class="steps-container">
            <div class="step">
                <div class="step-number">1</div>
                <div class="step-title">Send us a photo of your sofa for assessment</div>
                <div class="step-content">
                    Take a clear photo of your furniture and send it to us for a quick evaluation and quote.
                    <div class="rates">
                        <div class="rate-item">3 Piece Sofa Set @ 1700PHP</div>
                        <div class="rate-item">Sofa @ 1200PHP</div>
                        <div class="rate-item">Chair @ 600PHP</div>
                    </div>
                    <div style="margin-top: 12px; font-size: 14px; color: #6b7280;">
                        For pieces outside of standard sizing, we send you a message of how much the service cost will be.
                    </div>
                </div>
            </div>
            
            <div class="step">
                <div class="step-number">2</div>
                <div class="step-title">Fill up our booking request form</div>
                <div class="step-content">
                    Complete our simple booking form with your details, preferred schedule, and service requirements. We'll confirm your appointment within 24 hours.
                </div>
            </div>
        </div>
    </div>
    
    <button class="download-btn" onclick="downloadImage()">Download PNG</button>
    
    <script>
        function downloadImage() {
            const element = document.getElementById('graphic');
            
            // Create canvas
            const canvas = document.createElement('canvas');
            const ctx = canvas.getContext('2d');
            
            // Set canvas size to 1:1 ratio (square)
            canvas.width = 1080;
            canvas.height = 1080;
            
            // Create a temporary container with exact dimensions
            const tempContainer = element.cloneNode(true);
            tempContainer.style.width = '1080px';
            tempContainer.style.height = '1080px';
            tempContainer.style.position = 'absolute';
            tempContainer.style.left = '-9999px';
            tempContainer.style.top = '-9999px';
            document.body.appendChild(tempContainer);
            
            // Use html2canvas alternative approach
            html2canvas(tempContainer, {
                width: 1080,
                height: 1080,
                scale: 1,
                backgroundColor: null,
                useCORS: true,
                allowTaint: true
            }).then(canvas => {
                // Create download link
                const link = document.createElement('a');
                link.download = 'limpio-ph-process.png';
                link.href = canvas.toDataURL('image/png');
                link.click();
                
                // Clean up
                document.body.removeChild(tempContainer);
            }).catch(error => {
                console.error('Error generating image:', error);
                // Fallback method
                fallbackDownload();
            });
        }
        
        function fallbackDownload() {
            // Alternative download method
            const element = document.getElementById('graphic');
            const rect = element.getBoundingClientRect();
            
            // Create a new window with the graphic
            const newWindow = window.open('', '_blank');
            newWindow.document.write(`
                <!DOCTYPE html>
                <html>
                <head>
                    <title>Limpio PH Process - Right click to save image</title>
                    <style>
                        body { margin: 0; padding: 20px; background: #f0f0f0; display: flex; justify-content: center; align-items: center; min-height: 100vh; }
                        .instruction { text-align: center; margin-bottom: 20px; font-family: Arial, sans-serif; }
                    </style>
                </head>
                <body>
                    <div>
                        <div class="instruction">
                            <h3>Right-click on the image below and select "Save image as..." to download</h3>
                        </div>
                        ${element.outerHTML}
                    </div>
                </body>
                </html>
            `);
        }
    </script>
    
    <!-- Include html2canvas library -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
</body>
</html>
